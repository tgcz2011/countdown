# 安装指南

## 只需要做一件事：配置 `.env`

### 步骤1：创建环境变量文件

复制 `.env.example` 为 `.env`，填写你的配置：

```bash
cp .env.example .env
```

`.env` 内容（所有敏感信息都在这里，**不要提交到 git**）：

```
DB_HOST=你的数据库主机
DB_USERNAME=你的数据库用户名
DB_PASSWORD=你的数据库密码
DB_NAME=你的数据库名
DB_CHARSET=utf8mb4
ADMIN_PASSWORD=你的管理密码
ADMIN_SESSION_TIMEOUT=86400
```

**注意**：
- 数据库用户需要有 `CREATE DATABASE` 和 `CREATE TABLE` 权限（免费云主机通常都有）
- `ADMIN_PASSWORD` 必填，留空会导致管理员登录全部被拒绝（安全设计，无默认密码）

### 步骤2：上传文件

用FTP或文件管理器，将整个项目文件夹上传到网站根目录。
**不要上传** `.env.example`、`_ftp_*.py`、调试脚本等（见 `.gitignore`）。

### 步骤3：访问页面

在浏览器打开任意一个页面，系统自动完成建库建表：

- `index.php` - 主页面（天:时:分:秒）
- `seconds.php` - 秒数页面
- `submit.php` - 名言投稿
- `edit.php` - 管理员配置（首次访问会要求登录）
- `admin/review.php` - 名言审核

## 不需要做的事

❌ 不需要手动导入SQL文件（自动建表）
❌ 不需要用phpMyAdmin创建表
❌ 不需要执行任何命令行命令

## 可能出现的情况

### 情况1：第一次打开显示空白或错误
这是正常的！数据库正在自动创建。**刷新页面**即可。

### 情况2：数据库连接失败
检查 `.env` 中的配置是否正确：
- 主机名：通常是 `localhost` 或云主机提供的内网IP
- 用户名/密码：在云主机控制台查看
- 数据库名：可自定义，如 `countdown_2024`

### 情况3：权限错误
某些免费主机需要手动创建数据库：
1. 登录主机控制台，手动创建 MySQL 数据库
2. 在 `.env` 中填写该数据库名
3. 刷新页面，系统会自动创建表

## 验证安装

访问 `edit.php`，用 `.env` 中配置的管理密码登录。能打开设置界面即安装成功。

## 修改配置

- **倒计时目标日期**：edit.php 的"同步设置"区域
- **倒计时样式**：对应标签页修改字体、颜色、背景
- **励志话语**：编辑框输入，用 `|` 分隔多条

所有修改实时生效，无需重启服务。

## 安全部署检查清单

- [ ] `.env` 未提交到版本控制，且服务器上权限为 600
- [ ] 已轮换所有曾公开过的密码（数据库 / FTP / 管理员）
- [ ] 服务器上没有遗留调试脚本（test_connection.php、debug_*.php、font_debug.php 等）
- [ ] 后台路径未公开（edit.php、admin/review.php 靠密码保护，建议再加目录访问限制）

## 需要帮助？

检查：
1. PHP版本 >= 7.0（推荐 7.4+）
2. PDO 扩展已启用（pdo_mysql）
3. DOM 扩展已启用（HtmlSanitizer 依赖，php-dom）
4. 数据库连接信息正确
5. 文件路径权限正确（一般755即可）
